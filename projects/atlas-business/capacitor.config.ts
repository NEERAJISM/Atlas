import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.atlas.business',
  appName: 'atlas-business',
  webDir: '../../dist/atlas-business',
  bundledWebRuntime: false
};

export default config;
