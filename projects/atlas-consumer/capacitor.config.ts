import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.atlas.consumer',
  appName: 'atlas-consumer',
  webDir: '../../dist/atlas-consumer',
  bundledWebRuntime: false
};

export default config;
